on:
  pull_request:
  push:
    branches:
      - dev
  
jobs:
  run_pull:
    name: run pull
    runs-on: ubuntu-latest
    
    steps:
    - name: test print
      run: echo ${{ secrets.TEST_SECRET }}
